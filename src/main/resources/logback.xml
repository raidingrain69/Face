<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console appender (useful during development) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} – %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender: logs go to %APPDATA%\FaceUnlocker\logs\ -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${APPDATA:-${user.home}/.faceunlocker}/FaceUnlocker/logs/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Daily rollover; keep 7 days -->
            <fileNamePattern>
                ${APPDATA:-${user.home}/.faceunlocker}/FaceUnlocker/logs/app-%d{yyyy-MM-dd}.log
            </fileNamePattern>
            <maxHistory>7</maxHistory>
            <totalSizeCap>50MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} – %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Suppress verbose OpenCV / JavaCV / JNA debug output -->
    <logger name="com.sun.jna"        level="WARN"/>
    <logger name="org.bytedeco"       level="WARN"/>
    <logger name="org.opencv"         level="WARN"/>

</configuration>
